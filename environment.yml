name: occpm
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      - django
      - pandas
      - plantuml
      - pm4py
      - pm4pymdl
      - requests
      - setuptools
      - python-decouple
      - black
      - pre-commit
      - plotly
      - filehash
      - graphviz
prefix: /home/jan/anaconda3/envs/occpm
